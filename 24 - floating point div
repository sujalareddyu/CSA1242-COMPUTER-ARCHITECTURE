#include <stdio.h>

int main() {
    float dividend = 10.0;
    float divisor = 3.0;
    float quotient;
    quotient = dividend / divisor;
    printf("The quotient of %f divided by %f is %f\n", dividend, divisor, quotient);

    return 0;
}
